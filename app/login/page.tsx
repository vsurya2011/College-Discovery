'use client';
import Link from 'next/link';import {useContext,useState} from 'react';import {useSearchParams} from 'next/navigation';import {AuthContext} from '@/components/auth-provider';
export default function Login(){const {refresh}=useContext(AuthContext);const params=useSearchParams();const next=params.get('next')||'/';const [email,setEmail]=useState('demo@collegediscovery.dev');const [password,setPassword]=useState('Demo@12345');const [err,setErr]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setErr('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok){setErr(d.error||'Login failed');return;}refresh();window.location.href=next;}return <main className="container" style={{padding:'70px 0',maxWidth:520}}><div className="card" style={{padding:24}}><h1>Welcome back</h1><p className="muted">Login to save colleges, comparisons and participate in Q&A.</p><form onSubmit={submit} style={{display:'grid',gap:12}}><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" required/><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" required/><button className="btn btn-primary">Login</button>{err&&<div style={{color:'#b91c1c'}}>{err}</div>}</form><p className="muted" style={{fontSize:13}}>Demo: demo@collegediscovery.dev / Demo@12345</p><p>New here? <Link href="/signup" style={{color:'#2563eb'}}>Create an account</Link></p></div></main>}
