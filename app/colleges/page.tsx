import { CollegeSearch } from '@/components/college-search';
export default function CollegesPage(){return <main className="container" style={{padding:'34px 0'}}><h1 style={{marginBottom:6}}>College listings</h1><p className="muted" style={{marginTop:0,marginBottom:24}}>Search and filter a real dataset stored in PostgreSQL.</p><CollegeSearch/></main>}
