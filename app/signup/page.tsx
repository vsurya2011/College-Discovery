'use client';
import Link from 'next/link';import {useContext,useState} from 'react';import {AuthContext} from '@/components/auth-provider';
export default function Signup(){const {refresh}=useContext(AuthContext);const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [err,setErr]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setErr('');const r=await fetch('/api/auth/signup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,password})});const d=await r.json();if(!r.ok){setErr(d.error||'Signup failed');return;}refresh();window.location.href='/';}return <main className="container" style={{padding:'70px 0',maxWidth:520}}><div className="card" style={{padding:24}}><h1>Create account</h1><p className="muted">Start building your own college shortlist.</p><form onSubmit={submit} style={{display:'grid',gap:12}}><input className="input" value={name} onChange={e=>setName(e.target.value)} placeholder="Full name" required minLength={2}/><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" required/><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password (8+ chars)" required minLength={8}/><button className="btn btn-primary">Create account</button>{err&&<div style={{color:'#b91c1c'}}>{err}</div>}</form><p>Already have an account? <Link href="/login" style={{color:'#2563eb'}}>Login</Link></p></div></main>}
